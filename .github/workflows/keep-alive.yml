name: Supabase Keep Alive

on:
  schedule:
    - cron: "*/10 * * * *" # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call Supabase Function
        run: curl -s https://smaaggdtufiiojvxxdzc.functions.supabase.co/keep-alive